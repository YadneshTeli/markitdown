{
  "name": "markitdown-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/markitdown-ts",
    "packages/markitdown-mcp-ts",
    "packages/markitdown-sample-plugin-ts",
    "packages/markitdown-ocr-ts"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --if-present --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
